<div class="container">
  <div class="row">
    <div class="col-10">
      <h1>Book</h1>
      <table class="table">
        <thead>
          <tr>
            <th></th>
            <th>title</th>
            <th>Option</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>
              <%= @user.image %>
              <%= link_to @user.name, user_path(@user) %>
            </td>
            <td><%= link_to @book.title, book_path(book.id) %> </td>
            <td><%= @book.body %></td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>
</div>